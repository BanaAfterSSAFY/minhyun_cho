---
title: "[리뷰] G2_3109_빵집.java"
date: 2026-02-10
tags: [Algorithm, Java]
---

안녕하세요, 알고리즘과 자료구조의 세계를 탐험하는 여러분! 🚀 오늘은 백준 G2 난이도의 "빵집" 문제를 파이프라인 설치 문제로 재해석하여, 탐욕적인(Greedy) DFS 접근 방식의 정수를 살펴보는 시간을 갖겠습니다. G2 난이도지만 핵심 아이디어만 잘 파악하면 효율적으로 풀 수 있는 문제입니다.

---

## 🍞 백준 3109번: 빵집 (G2) 문제 파헤치기

### 1. 문제 요약 및 풀이 전략

이 문제는 빵집으로 가는 파이프라인을 최대한 많이 설치하는 것이 목표입니다. 조건은 다음과 같습니다:
*   `R`행 `C`열의 그리드가 주어집니다.
*   각 칸은 빈 칸(`.`)이거나 건물(`x`)입니다.
*   파이프는 첫 번째 열에서 시작하여 마지막 열까지 연결되어야 합니다.
*   파이프는 오른쪽 위, 오른쪽, 오른쪽 아래 세 방향으로만 이동할 수 있습니다. (즉, `(r-1, c+1)`, `(r, c+1)`, `(r+1, c+1)`)
*   한 번 파이프가 설치된 칸은 다른 파이프가 사용할 수 없습니다. (비-겹침 조건)
*   최대한 많은 파이프를 설치해야 합니다.

**💡 핵심 풀이 전략: 탐욕적인 DFS (Greedy DFS)**

이 문제의 핵심은 "탐욕적으로" 파이프를 설치하는 것입니다. 왜 탐욕적 접근이 가능할까요? 파이프는 오직 오른쪽으로만 진행하기 때문에, **한 번 설치된 파이프는 다른 파이프의 경로에 영향을 줄 수 없습니다.** 즉, 특정 파이프가 자신의 경로를 정했을 때, 그 결정이 나중에 시작하는 다른 파이프의 최적 경로를 방해하지 않습니다.

따라서 우리는 다음과 같은 전략을 사용합니다:
1.  **첫 번째 열의 각 행 (0부터 R-1까지)에서 시작점을 잡고 DFS를 시도합니다.**
2.  **DFS 탐색 우선순위:** 현재 위치에서 다음 칸으로 이동할 때, `오른쪽 위` -> `오른쪽` -> `오른쪽 아래` 순서로 탐색합니다.
    *   이 순서가 중요한 이유는 **위쪽 경로를 먼저 확보하면 아래쪽 경로를 다른 파이프가 사용할 수 있도록 남겨둘 가능성이 높아지기 때문입니다.** (가장 위쪽 파이프가 위쪽 길을 차지하면, 가운데/아래쪽 파이프는 그만큼 자유롭게 아래쪽 길을 쓸 수 있습니다.)
3.  **경로 표시:** 한 번 파이프가 지나간 칸은 다시는 다른 파이프가 사용하지 못하도록 `grid`에 `x`로 표시합니다. (visited 배열 대신 grid 자체를 변경하는 방식)
4.  **성공 시 즉시 종료:** 만약 DFS 탐색 중 마지막 열(`C-1`)에 도달하면, 해당 파이프 설치는 성공한 것이므로 즉시 `true`를 반환하며 탐색을 중단하고 다음 시작점으로 넘어갑니다. 이는 불필요한 탐색을 줄여주는 강력한 최적화입니다.

### 2. 시간 복잡도와 공간 복잡도 분석

*   **시간 복잡도: O(R \* C)**
    *   `main` 함수에서 `R`번의 루프를 돌며 각 행에 대해 `dfs`를 호출합니다.
    *   각 `dfs` 호출 내에서는 현재 위치에서 3방향을 탐색합니다.
    *   중요한 점은, 한 번 방문한 (`x`로 표시된) 셀은 다시 탐색하지 않는다는 것입니다. 파이프가 성공적으로 설치되면 해당 경로의 모든 셀은 `x`가 되고, 다른 `dfs` 호출은 그 셀을 지나갈 수 없습니다. 따라서 `dfs`는 각 셀을 최대 한 번만 방문하며 성공 여부를 결정합니다.
    *   결론적으로, 모든 셀을 한 번씩 탐색하는 데 드는 시간은 `O(R * C)`가 됩니다.
*   **공간 복잡도: O(R \* C)**
    *   `grid` 배열을 저장하는 데 `O(R * C)` 공간이 필요합니다.
    *   재귀 호출 스택의 깊이는 최악의 경우 파이프의 길이가 `C`이므로 `O(C)`입니다.
    *   따라서 전체 공간 복잡도는 `O(R * C)`가 됩니다.

문제의 `R, C` 최대값이 `10000, 500`이므로 `R * C`는 `5 * 10^6` 수준입니다. 이 정도면 `O(RC)` 시간 복잡도는 넉넉하게 통과할 수 있습니다.

### 3. 코드에서 개선할 점이나 주의해야 할 예외 케이스(Edge Case)

현재 코드는 이 문제의 핵심을 정확히 꿰뚫는 매우 효율적이고 깔끔한 솔루션입니다. 몇 가지 짚어볼 점들이 있습니다.

1.  **방문 여부 및 경로 표시 (`grid[x][y] = 'x'`)**:
    *   `dfs` 함수 진입 시 `grid[x][y] = 'x'`를 통해 현재 칸을 '방문했음'과 동시에 '파이프가 지나감'을 표시합니다.
    *   **주의할 점**: 이 문제에서는 파이프가 오직 오른쪽으로만 이동하고, 한 번 지나간 길은 재사용 불가하다는 조건 때문에 `x`로 표시된 칸을 **다시 원래 상태(`.'`)로 되돌리는 (백트래킹) 작업이 필요 없습니다.** 만약 양방향 이동이 가능하거나, 경로를 재사용할 수 있는 다른 문제였다면, `dfs` 호출 후 `grid[x][y] = '.'`처럼 원상 복구하는 백트래킹 로직이 필수적이었을 것입니다. 이 문제는 이 특성 덕분에 코드가 더 간결해질 수 있습니다.

2.  **DFS 탐색 방향 우선순위**:
    *   `dirR = {-1, 0, 1}` 순서로 `오른쪽 위` -> `오른쪽` -> `오른쪽 아래`를 탐색하는 것은 이 문제의 **탐욕적 전략을 구현하는 핵심**입니다. 위쪽을 먼저 탐색하여 성공하면 그 경로를 고정하고, 다른 경로들이 아래쪽 공간을 활용하도록 유도합니다. 이 순서가 바뀌면 최적의 해를 찾지 못할 수 있습니다.

3.  **기저 조건 및 조기 종료 (`if (y == C - 1) return true;` 와 `if (dfs(dx, dy)) return true;`)**:
    *   현재 코드는 마지막 열에 도달하면 즉시 `true`를 반환하고, 재귀 호출 체인을 따라 모든 부모 호출이 `true`를 반환하게 만듭니다. 이는 하나의 파이프 경로가 성공하면 더 이상 현재 시작점에서 다른 경로를 탐색할 필요가 없음을 의미하며, 불필요한 연산을 줄이는 **아주 중요한 최적화**입니다.

4.  **경계 조건 (`dx >= 0 && dx < R && dy < C`)**:
    *   `dy < C` 조건은 사실 `y == C - 1` 기저 조건에 의해 대부분 처리되므로 필수는 아니지만, 안전한 코드 작성을 위해 포함된 좋은 습관입니다. `dy`는 항상 `y+1`이므로, `y`가 `C-1`일 때는 이미 마지막 열에 도달한 것이고, `y`가 `C-2`일 때 `dy`는 `C-1`이 되어 마지막 열을 탐색하게 됩니다. `dy`가 `C`가 될 일은 없지만, 이런 방어적인 코드는 나쁘지 않습니다.

5.  **입력 처리**:
    *   `BufferedReader`와 `StringTokenizer`를 사용하여 대량의 입력을 빠르게 처리하는 것은 경쟁 프로그래밍에서 좋은 습관입니다.

**예외 케이스 (Edge Cases):**

*   **R=1 (한 줄짜리 그리드):** `dfs(0,0)`이 호출되어 `grid[0][0]`이 `.`이면 `true` 반환, 아니면 `false` 반환. 정확하게 동작합니다.
*   **첫 번째 열이 모두 막혀있는 경우:** `dfs(i, 0)`이 `false`를 반환하므로 `res`는 0이 됩니다. 정확합니다.
*   **마지막 열까지 모든 길이 막혀있는 경우:** 중간에 갈 수 있는 길이 없으면 `dfs`는 결국 `false`를 반환합니다. 정확합니다.

---

### 결론

백준 3109번 "빵집" 문제는 탐욕적 DFS의 원리와 효율성을 잘 보여주는 좋은 문제입니다. 파이프가 한 방향으로만 이동하고 비-겹침 조건을 만족해야 한다는 점이 탐욕적 전략을 가능하게 하며, 올바른 탐색 우선순위와 조기 종료 최적화를 통해 `O(RC)`의 효율적인 해법을 이끌어낼 수 있습니다. 이 문제의 풀이를 통해 DFS, 백트래킹 (필요 없는 경우), 그리고 탐욕적 알고리즘의 적용 시점에 대한 이해를 한층 높이셨기를 바랍니다!

Happy Coding! 👨‍💻👩‍💻